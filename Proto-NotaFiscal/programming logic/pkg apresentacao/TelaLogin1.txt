import

define public class ClassName {
	textfield pass;
	button login;
	
	define gui fucntion {
		set frame parameters and conditions;
		
		set and place pass;
		set and place login;
		
		create action listener (
			when login pressed {
				hahsedPass = getPass(stateFile);
				if hash(pass.input) = hashedPass {
					open TelaPrincipal(hahsedPass);
					close this;
				}
			}
		)
	}
}
