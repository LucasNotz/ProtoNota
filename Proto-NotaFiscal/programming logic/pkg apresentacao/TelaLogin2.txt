
import
define TelaLogin2 class {
	textfield pass;
	textfield passAgain;
	label lblPass;
	label lblPassAgain;
	button create;
	
	define gui fuction {
		set frame parameters/conditions;
		
		set and place pass;
		set and place passAgain;
		set and place lblPass;
		set and place lblPassAgain;
		set and place create;
	
		create action listener{
			when button pressed {
				if pass.input = 10 digits {
					if pass.input = passAgain.input (
						insertPass(pass.input);
						"open" TelaPrincipal(getPass(stateFile);
						close this gui;
				} else {
					close
                    delete all folder so app can be run again
				}
			}
		}
	}
}

